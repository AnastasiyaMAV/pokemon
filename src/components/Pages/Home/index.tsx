import React from 'react';
import Header from '../../Header';
import s from './Home.module.scss';

const Home = () => {
  return (
    <div className={s.root}>
      <Header />
    </div>
  );
};

export default Home;
